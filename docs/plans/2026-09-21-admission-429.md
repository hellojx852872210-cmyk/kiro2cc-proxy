# Admission / 429 实施计划

Goal：按 docs/specs/2026-09-21-admission-429.md 修复准入和限流，保持实时输出、调度、402、模型映射和计价。
Architecture：原始RpmTracker保留展示，加入原子滑动窗口预留；响应包装拥有许可；typed限流跨provider/handler/OpenAI传播；每号refresh冷却避免误禁与串行重试。不要换整个调度器。
Tech Stack：Rust2024、Tokio、Axum0.8、Reqwest0.12，现有依赖。

## 任务一：原子RPM与限流类型

文件：src/model/rpm.rs；新增src/kiro/error.rs；src/kiro/mod.rs；src/kiro/endpoint.rs。

- [ ] 先新增并发准入limit8/16请求红灯测试，再实现 `try_reserve_credential` 一类原子接口。必须返回可测等待时间，不拆成先check后record。
- [ ] 添加Retry-After解析/typed来源测试，再实现秒/HTTP-date标准化；不新增crate。
- [ ] endpoint桶支持按上游给出的截止时间等待，并提供最早恢复时间；保留原无头30秒行为和端点隔离。
- [ ] 运行过滤测试，记录失败→通过命令和退出码。

## 任务二：许可完整生命周期与provider准入

文件：新增src/kiro/response.rs、src/kiro/admission.rs（若不需独立模块可在provider中实现）；src/kiro/provider.rs；src/model/config.rs；src/anthropic/handlers.rs；src/anthropic/websearch.rs。

- [ ] 添加hold-body、错误、EOF、cancel、global1搜索续轮测试。
- [ ] LeasedResponse包装原response+OwnedSemaphorePermit；status/headers只借用，text/bytes_stream消费后仍保留许可直到终止。
- [ ] 保留原API `(response,id)`形态，修改response类型及相关stream签名；禁止解包后遗失guard。
- [ ] 活跃许可只覆盖发包和读body；token/RPM/重试sleep不占；账号满尝试其他合格候选。
- [ ] 新旧配置兼容，队列数量和等待有界；test limits可小到global1，不影响生产默认50/20。
- [ ] RPM只在真正send前预留；删除成功后双记账；API与MCP一致。
- [ ] 运行provider/handler相关测试并写实际结果。

## 任务三：刷新429保护与错误端到端传播

文件：src/kiro/token_manager.rs；src/kiro/provider.rs；src/anthropic/handlers.rs；src/anthropic/websearch.rs；src/openai/handlers.rs。

- [ ] 模拟refresh429，对故障计数、禁用状态、第二请求发包次数写红灯测试。
- [ ] 三种认证刷新返回typed限流，各获取/强制刷新分支不记永久故障；每号冷却在refresh锁内二次检查。
- [ ] typed429优先映射，不让no-candidate/桶满覆盖为502；402全耗尽维持优先级。
- [ ] 普通Messages、/cc、Chat、Responses保留安全Retry-After；纯搜索不吞429；已开流明确error终止。
- [ ] 运行相关测试，并对原有sticky/round-robin/endpoint/invalid_grant/402回归。

## 任务四：真实HTTP mock与完整回归（Grok）

可新增：src/kiro/provider_tests.rs、src/kiro/admission_tests.rs（生产模块用cfg(test)引入）；scripts/test-admission-mock.py（如果集成测试需要，必须纯本地假凭据）。

- [ ] 真实provider发往loopback server，构造200 headers之后hold SSE、429+Retry-After、5xx、断连、取消和多轮MCP。
- [ ] `cargo test --locked` 全量，失败不得删断言/忽略；记录未受新改动影响的基线红灯。
- [ ] `cargo clippy --locked --all-targets -- -D warnings`；`cargo fmt -- --check`。既有基线格式问题单独报告，禁止无关全仓改写。
- [ ] 不改admin-ui/user-ui、converter、cache、usage、模型表，除非现有签名编译必需且先在回执说明。
- [ ] 原子写回执、导出相对派工基线的补丁，停止改动等待review。

## 任务五：独立验收、镜像和发布（主会话独占）

- [ ] cmux-crew accept收真实diff+测试；独立reviewer审并发、cancel、429传播，修复发现项并局部复验。
- [ ] Docker镜像构建，保留锁定源码commit、平台、digest；容器内跑相关测试及release镜像启动/鉴权/静态资源检查。禁止候选容器挂生产可写数据或真实出网。
- [ ] GitHub authenticated fork独立分支，检查公开补丁和文档不带凭据/客户数据；不向上游发PR。
- [ ] 用户给付费真冒烟预算后，核对部署锁、现网CAS、备份及rollback脚本，候选先原路径小流量验收。
- [ ] 冻结镜像替换目标容器，其他compose服务不动；旧镜像可回滚。沿原客户路径复现并观察错误/延迟/完整流，失败回滚。
- [ ] PROGRESS记录真实通过范围、剩余风险及未执行项；不将测试镜像通过写成生产已修复。
