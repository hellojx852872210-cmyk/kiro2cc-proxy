# Admission / 429 候选版本验收记录

状态：**本地实现与隔离镜像验收通过；尚未生产发布或真实 Kiro 冒烟。**

## 冻结产物

- 源码提交：`534979906c2ae8406acacb7f1f0380fc6e091d67`
- 镜像标签：`kiro2cc-hardening:534979906c2ae8406acacb7f1f0380fc6e091d67`
- 原生平台：`linux/amd64`
- CI镜像ID：`sha256:e00bb1a8b07d6a9f9f594e114945cff5a1bb759eb77c77a7e25a89c66efe6f2b`
- 镜像归档SHA256：`d5030a9557ce7d88d18ce171c5f0f6d5e9d5a0101afc636ea0070a100d9183f8`
- 二进制SHA256：`6d7a68af181d485050f02abe1947839dbf63d86fec094342e262d21dd2ac5df1`
- [原生构建及镜像验收](https://github.com/hellojx852872210-cmyk/kiro2cc-proxy/actions/runs/35602550928)
- [设计与验收规格](../specs/2026-09-21-admission-429.md)

后续文档提交不改变上述已验收产物；发布应使用这份冻结归档，不因文档变化重新构建不同镜像。

## 变更

1. 账号RPM改为真实发送前原子预留，包含失败尝试，成功后不双记。
2. 响应携带全局/账号许可，覆盖完整body/SSE，EOF、读错、取消时释放。
3. 类型化429、真实Retry-After、分作用域冷却与正确终态优先级；有效等待头不被慢错误正文拖住。
4. 刷新429不误禁；token准备任务独立有界，短准入超时/客户取消不丢已发OAuth轮换结果。
5. 纯/混合搜索不吞限流为200；取消不留后台推理；非流续轮截断返回502。

保留原轮询、会话绑定、无头429端点fallback、原始绑定范围月额度耗尽402、fable别名及cache-split计价。21个cache/converter/stream/usage文件与部署基线字节相同。

## 验证

- 修改前基线：680项 Rust 测试通过。
- 最终主会话及原生Linux CI：`cargo test --locked` **740/740**。
- Release镜像：**31/31**，真实二进制运行在 Docker internal 网络，仅假凭据、本地受信测试CA与模拟Kiro，无外网出口/真实模型调用。
- 修改的Rust文件格式检查通过。
- 严格clippy仍有原基线93条诊断；按文件/函数/诊断类型对照，本次新增0。没有宣称全仓lint全绿，也未顺手格式化未改的计价文件。

镜像测试关键结果：

| 验收项 | 结果 |
|---|---|
| 16并发、单号RPM8 | 8成功、8本地429，上游恰好8次 |
| 同账号21条长流、上限20 | 20准入、1限流，最大活跃20 |
| 全局/账号槽取消释放 | 下次有效内容约0.25秒恢复 |
| Messages、/cc、Chat、Responses | 实时增量及完整终态保留，Retry-After 37均透传 |
| 429头+慢错误body | API/MCP约0.12秒返回，不等正文EOF |
| 刷新429 | 不误禁、不重复刷新，冷却后恢复 |
| OAuth超过准入期限 | 客户约0.47秒返回，后台保存新token；过期请求不续发推理 |
| 全端点冷却 | 仍429，不变成502 |
| 无头429端点fallback | 同号两个端点，最终200 |
| 硬A+软B候选 | 不越过A的硬门，B备用端点成功 |
| 月额度终态 | 原范围全耗尽402；子集耗尽不覆盖原范围RPM429 |
| global1多轮搜索 | 完整完成，无自锁 |
| MCP在途取消 | 约0.16秒停止，无模型续轮 |
| 搜索限流/截断 | 正确429/SSE error/502，不伪造成功 |

Grok通过cmux-crew完成主体及审计返工；主会话核验实际diff、补齐独立镜像套件，并以两项先红后绿回归修正最后的子集额度作用域问题。

## 发布前仍需完成

- 取得真实冒烟的明确费用/credits上限，并沿实际客户链路检查。
- 重新核对当前生产源码、镜像、配置及连接目标；本轮只读快照不能覆盖其他会话后续修改。
- 取得项目服务器发布锁，备份当前镜像/配置；仅替换目标服务，不改账号、额度、计价、卷和相邻服务。
- 失败回滚镜像；不得用旧凭据/账本快照覆盖期间已更新的数据。

**行为提醒：**原设计50/20并发上限现在覆盖完整流；账号RPM统计现在包含尝试而非只统计成功。因此本地429可能比过去更早、更准确，不能只看单一429总数评价效果。Redis/多实例统一准入和新负载算法未加入。本记录不证明真实云端吞吐提升比例、真实缓存收益或消除所有上游限流。
